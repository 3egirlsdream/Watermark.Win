<span class="wm-membership-mark @(Compact ? "is-compact" : null)" aria-hidden="true">
    <WmIcon Icon="crown" Weight="regular" Size="32" />
</span>

@code {
    [Parameter] public bool Compact { get; set; }
}
