<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <title>轻影 Litograph｜摄影模板设计与专业影像处理</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="轻影 Litograph 是面向摄影师的跨平台影像工作台，支持 AI 技能生成水印模板、EXIF、16-bit 调色、RAW、多帧合成与批量导出。" />
    <base href="/" />
    @*<link rel="stylesheet" href="bootstrap/bootstrap.min.css" />*@
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="Watermark.Web.styles.css" />
    <link href="_content/Masa.Blazor/css/masa-blazor.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/@@mdi/font@7.4.47/css/materialdesignicons.min.css" rel="stylesheet" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <link rel="stylesheet" href="homepage.css?v=20260722b" />
    <HeadOutlet />
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    <script src="_content/Masa.Blazor/js/masa-blazor.js"></script>
    <script src="homepage.js?v=20260715b"></script>
</body>

</html>
<script>
    window.getDimensions = function () {
        return {
            width: window.innerWidth,
            height: window.innerHeight
        };
    };
    window.saveAsFile = function(filename, byteArray) {
    // 创建一个Blob对象
    var blob = new Blob([new Uint8Array(byteArray)], { type: "application/octet-stream" });

    // 创建一个虚拟链接
    var link = document.createElement('a');

    // 设置链接属性，包括文件名和URL
    link.href = window.URL.createObjectURL(blob);
    link.download = filename;

    // 模拟点击这个链接来触发下载操作
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);

        
}
</script>
