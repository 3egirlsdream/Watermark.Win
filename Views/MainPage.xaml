<Page x:Class="JointWatermark.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JointWatermark"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">
    <Page.Resources>
        <Style TargetType="Border" x:Key="BorderStyle">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#FFF" Opacity="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" ShadowDepth="1" BlurRadius="10" Opacity="0.2" />
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="1"></Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Border Style="{StaticResource BorderStyle}">
                <TabControl  VerticalContentAlignment="Top" materialDesign:ColorZoneAssist.Mode="Standard"  materialDesign:ShadowAssist.ShadowDepth="Depth1" Style="{StaticResource MaterialDesignNavigationRailTabControl}">
                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Width="auto" Height="auto">
                                <materialDesign:PackIcon Width="16" Height="16" HorizontalAlignment="Center" Kind="Folder" />
                                <TextBlock  HorizontalAlignment="Center" Text="图片" />
                            </StackPanel>
                        </TabItem.Header>
                        <Grid Grid.Row="0" Width="auto" Height="auto" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="30"/>
                                </Grid.RowDefinitions>
                                <materialDesign:PackIcon x:Name="plus1" Kind="Plus" Height="auto" Width="auto" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30"/>
                                <ListBox HorizontalContentAlignment="Stretch" x:Name="listbox" ItemsSource="{Binding Images, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedImage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="Transparent">
                                                <Border.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.CmdClickItem, RelativeSource={ RelativeSource AncestorType=Page}}" CommandParameter="{Binding ID}"/>
                                                </Border.InputBindings>
                                                <StackPanel Orientation="Horizontal" Height="40" >
                                                    <Image Height="40" Width="40" Source="{Binding Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Label Height="40" Width="auto" Margin="20 0" Content="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>

                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Separator Grid.Row="1" Style="{StaticResource MaterialDesignDarkSeparator}" />

                                <Button
                        Click="ListBox_SelectionChanged"
                        Grid.Row="2"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                        Width="30"
                        Padding="2 0 2 0"
                        materialDesign:RippleAssist.IsCentered="True" Cursor="Hand">
                                    <materialDesign:PackIcon Kind="FolderOpen" />
                                </Button>
                            </Grid>

                        </Grid>
                    </TabItem>
                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Width="auto" Height="auto">
                                <materialDesign:PackIcon Width="16" Height="16" HorizontalAlignment="Center" Kind="Settings" />
                                <TextBlock  HorizontalAlignment="Center" Text="配置" />
                            </StackPanel>
                        </TabItem.Header>
                        <StackPanel Grid.Row="2">
                            <Grid Margin="10 5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="显示品牌名" VerticalAlignment="Center"/>
                                <ToggleButton x:Name="showCor" Grid.Column="1" Style="{StaticResource MaterialDesignSwitchToggleButton}" VerticalAlignment="Center" DockPanel.Dock="Right" IsChecked="True" Checked="CheckShowProducer" Unchecked="CheckShowProducer"/>
                            </Grid>
                            <TextBox materialDesign:HintAssist.Hint="左侧第一行(设备名)" Text="{Binding SelectedImage.Config.LeftPosition1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Cursor="Arrow" x:Name="deviceName" Margin="10 5" TextChanged="deviceName_TextChanged"  materialDesign:HintAssist.IsFloating="True"/>
                            <TextBox x:Name="xy" Text="{Binding SelectedImage.Config.LeftPosition2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="左侧第二行"  Margin="10 5" TextChanged="deviceName_TextChanged"  materialDesign:HintAssist.IsFloating="True"/>
                            <TextBox x:Name="mount" materialDesign:HintAssist.Hint="右侧第一行" Text="{Binding SelectedImage.Config.RightPosition1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Margin="10 5" TextChanged="deviceName_TextChanged"  materialDesign:HintAssist.IsFloating="True"/>
                            <TextBox materialDesign:HintAssist.Hint="右侧第二行" Text="{Binding SelectedImage.Config.RightPosition2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Margin="10 5" TextChanged="deviceName_TextChanged"  materialDesign:HintAssist.IsFloating="True"/>
                            <StackPanel Margin="10 5" HorizontalAlignment="Left" Orientation="Horizontal">
                                <TextBox materialDesign:HintAssist.Hint="底色" TextChanged="deviceName_TextChanged" Text="{Binding ElementName=colorpicker, Path=Color}" IsReadOnly="True"  materialDesign:HintAssist.IsFloating="True"/>
                                <Button x:Name="colorBtn" Click="colorBtn_Click" Margin="10 0" Cursor="Hand" Style="{StaticResource MaterialDesignIconButton}" Height="30" Width="30" VerticalAlignment="Center" VerticalContentAlignment="Center">
                                    <materialDesign:PackIcon Kind="Palette" />
                                </Button>
                                <Popup x:Name="popup" Width="300" Height="250" IsOpen="False" Placement="Top" PlacementTarget="{Binding ElementName=colorBtn}" StaysOpen="False">
                                    <materialDesign:ColorPicker x:Name="colorpicker" Color="{Binding Color, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Popup>
                            </StackPanel>


                        </StackPanel>
                    </TabItem>
                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Width="auto" Height="auto">
                                <materialDesign:PackIcon Width="16" Height="16" HorizontalAlignment="Center" Kind="Watermark" />
                                <TextBlock  HorizontalAlignment="Center" Text="LOGO" />
                            </StackPanel>
                        </TabItem.Header>
                        <WrapPanel>
                            <WrapPanel x:Name="logoes">

                            </WrapPanel>
                            <WrapPanel Orientation="Horizontal" x:Name="addp">
                                <materialDesign:Card x:Name="add" MouseLeftButtonDown="Card_MouseLeftButtonDown_1" Cursor="Hand" ToolTip="Logo图片复制到根目录logo文件夹，将自动出现在这里"
                        Margin="12 0"
                        Width="60"
                        Height="60"
                        materialDesign:ShadowAssist.ShadowDepth="Depth1">
                                    <Grid>
                                        <materialDesign:PackIcon x:Name="plus" Kind="Plus" Height="40" Width="40" Margin="10"/>
                                        <Image x:Name="selfmade" Visibility="Collapsed" Source="canon.png" Height="40" Width="40"/>
                                    </Grid>
                                </materialDesign:Card>
                            </WrapPanel>
                        </WrapPanel>
                    </TabItem>
                </TabControl>
            </Border>
           
            <!--<Button Grid.Row="1" Width="120" Height="30" Content="选择本地图片" Click="SelectPictureClick"/>-->
           

           
        </Grid>
        <Grid Grid.Column="1">
            <Button Click="Button_Click"
              Style="{StaticResource MaterialDesignFloatingActionSecondaryDarkButton}"
              ToolTip="合成图片"
              materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding Loading, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
              materialDesign:ButtonProgressAssist.Value="-1"
              materialDesign:ButtonProgressAssist.IndicatorForeground="#408400"
              materialDesign:ButtonProgressAssist.IsIndeterminate="True">
                <materialDesign:PackIcon
                    Kind="Play"
                    Height="40"
                    Width="40" />
            </Button>
        </Grid>
        <Border Grid.Column="1" Style="{StaticResource BorderStyle}" Margin="5 0">
            <Grid Background="#e6e6e6">
                <Image x:Name="createdImg" Width="{Binding ElementName=grid, Path=Width}" Height="{Binding ElementName=grid, Path=Height}" Source="../Resources/github.png" Margin="50" Stretch="None"/>
                <ListBox x:Name="listbox2" ItemsSource="{Binding OutputImages, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Height="40">
                                <Image Height="40" Width="40" Source="{Binding Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Height="40" Width="auto" Margin="20 0" Content="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
               
                <Button Grid.Row="1" Width="100" Height="30" Click="downloadClick" Visibility="Collapsed">
                    <materialDesign:PackIcon Kind="FolderOpen"/>
                </Button>

                <GroupBox Visibility="Collapsed"
                  Grid.Row="2"
                  Header="预览水印"
                  Style="{StaticResource MaterialDesignGroupBox}"
                  Margin="20 20"
                  materialDesign:ShadowAssist.ShadowDepth="Depth3"
                  BorderThickness="11">
                    <materialDesign:Card
                Grid.Column="2"
                Width="auto"
                Height="50"
                materialDesign:ShadowAssist.ShadowDepth="Depth1">
                        <Image x:Name="preveiew"/>
                    </materialDesign:Card>
                </GroupBox>
            </Grid>
        </Border>
       
        <materialDesign:Snackbar Grid.ColumnSpan="3" x:Name="SnackbarThree" Message="出错了，请重试"/>
    </Grid>
</Page>
