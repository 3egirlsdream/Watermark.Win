<Window x:Class="JointWatermark.Views.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JointWatermark.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Window1" Height="450" Width="800">
    <Grid Background="LightGray">
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource MaterialDesignIconButton}" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top"  Click="Button_Click">
                <materialDesign:PackIcon Kind="AllInclusive" />
            </Button>
            <Button Style="{StaticResource MaterialDesignIconButton}" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top" Click="Button_Click_1">
                <materialDesign:PackIcon Kind="Settings" />
            </Button>
        </StackPanel>
       
        <Image x:Name="img" Margin="10"/>
    </Grid>
</Window>
